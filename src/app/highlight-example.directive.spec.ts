import { HighlightExampleDirective } from './highlight-example.directive';

describe('HighlightExampleDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightExampleDirective();
    expect(directive).toBeTruthy();
  });
});
